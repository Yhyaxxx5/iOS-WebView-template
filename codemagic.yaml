workflows:
  ios-webview-workflow:
    name: iOS WebView Build
    instance_type: mac_mini_m1
    max_build_duration: 30
    environment:
      xcode: latest
    scripts:
      - name: Initialize Project
        script: |
          echo "Preparing app build..."
    artifacts:
      - build/**/*.ipa
